[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "retail-dashboard"
version = "0.0.1"
description = "Retail Sales Performance Dashboard utilities"
authors = [ { name = "Your Name" } ]
dependencies = []
